import React from 'react';
import MapCard from './MapCard';

const Maps = () => {
  return (
    <section id="maps">
      {/* <MapsTitle>{titles.maps}</MapsTitle>
            <MapsWrapper>
                {maps.map(map => {
                    return <MapCard
                        image={map.image}
                        title={map.title}
                        monument={map.monument}
                    />
                })}
            </MapsWrapper> */}
    </section>
  );
};

export default Maps;
